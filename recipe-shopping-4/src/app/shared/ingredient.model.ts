export class Ingredient {
    // Typescript Shortcut- eliminating having to declare
    // members and write a constructor to perform assignments!
    constructor(public name: string, public quantity: number) { }
}